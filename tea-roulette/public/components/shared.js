export const teaPreferences = []; // Array to store the tea preferences
export let isSpinning = false; // Boolean to check if the wheel is currently spinning
export let currentRotation = 0; // Current rotation angle for the wheel
